apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "block-browser.fullname" . }}-test-connection"
  labels:
    app.kubernetes.io/name: {{ include "block-browser.name" . }}
    helm.sh/chart: {{ include "block-browser.chart" . }}
    app.kubernetes.io/instance: {{ .Release.Name }}
    app.kubernetes.io/managed-by: {{ .Release.Service }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "block-browser.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
