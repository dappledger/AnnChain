apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "validator.fullname" . }}-test-connection"
  labels:
    app.kubernetes.io/name: {{ include "validator.name" . }}
    helm.sh/chart: {{ include "validator.chart" . }}
    app.kubernetes.io/instance: {{ .Release.Name }}
    app.kubernetes.io/managed-by: {{ .Release.Service }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "validator.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
